const ApiError = require('../exceptions/api-error');
// const

class VendorService {
  async addProduct({ category, subCategoryId, image }) {
    try {
      console.log(image, subcategory);
    } catch (error) {

    }
  }
}

module.exports = new VendorService();
